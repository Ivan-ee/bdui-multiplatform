services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: arenda
      POSTGRES_PASSWORD: arenda
      POSTGRES_DB: arenda
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U arenda -d arenda"]
      interval: 3s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
